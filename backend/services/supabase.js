// Deprecated: this project no longer depends on Supabase. Kept as a stub to
// avoid breaking any legacy imports; prefer local filesystem storage via
// the /data static mount and the helpers in ./fastapi.js.

export const supabase = null;
